<h2>Rides</h2>

<div class="box">
  <% @rides.each do |ride| %>
    <div class="ride">
      <h3><%= ride.name %></h3>
      
      <% ride.routes.each do |route| %>
        <div class="route">
          <p>
            <strong><%= link_to route.name, ride_routes_path(route) %></strong> 
            <small><%= link_to "map", route.map_url %></small> <br/>
            <%= route.occurrence.begins_at.strftime("%l:%M") %> - <%= ride.location %> <br/> 
            <%= route.description %> 
          </p>          
        </div>
      <% end %>
      
    </div>
  <% end %>
</div>

