<% if @ride.routes.empty? %>
  <p class="center">Looks like there aren't any routes!</p>
<% else %>
  <table border="0" cellspacing="0" cellpadding="5">
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Map</th>
      <th></th>
    </tr>
    <%= render :partial => 'admin/routes/route', :collection => @ride.routes %>
  </table>
<% end %>
