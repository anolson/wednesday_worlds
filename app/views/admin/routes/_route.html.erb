<tr>
  <td><%= route.name %></td>
  <td><%= truncate(route.description) %></td>
  <td><%= link_to route.map_url, route.map_url %></td>
  <td><%= link_to "Edit", edit_admin_ride_route_path(route.ride, route) %> <%= link_to image_tag("delete.png"), admin_ride_route_path(route.ride, route), :method => :delete %></td>
</tr>