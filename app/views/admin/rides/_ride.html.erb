<tr>
  <td><%= ride.name %> (<%= link_to "routes", admin_ride_routes_path(ride) %>)</td>
  <td><%= ride.location %></td>
  <td><%= ride.currently_active ? 'Yes' : 'No' %></td>
  <td><%= link_to "Edit", edit_admin_ride_path(ride) %> <%= link_to image_tag("delete.png"), admin_ride_path(ride), :method => :delete %></td>
</tr>